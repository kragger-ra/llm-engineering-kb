---
id: "2.2a-sft"
section: "model"
title: "SFT: supervised fine-tuning, loss masking, LoRA/QLoRA на практике"
tags: ["sft", "fine-tuning", "loss-masking", "chat-template", "lora", "qlora", "peft", "target-modules", "adapter-merging", "packing"]
prereqs: ["2.1-pytorch-fluency", "2.5-gpu-minimum", "dswok:2.7-lora"]
dswok_refs: ["§2.7"]
sources:
  - "https://arxiv.org/abs/2106.09685"   # LoRA
  - "https://arxiv.org/abs/2305.14314"   # QLoRA (NF4, paged optimizer)
  - "https://huggingface.co/docs/trl/sft_trainer"   # TRL SFTTrainer, completion-only loss
  - "https://huggingface.co/docs/peft/main/en/conceptual_guides/lora"   # PEFT LoRA, target_modules
  - "https://raschka.com/blog/2023/llm-finetuning-lora.html"   # практические дефолты LoRA
difficulty: "intermediate"
last_reviewed: "2026-05-30"
volatile: false
---

# SFT: supervised fine-tuning, loss masking, LoRA/QLoRA на практике

Supervised fine-tuning (SFT, обучение с учителем на парах «инструкция → ответ») —
первый и чаще всего достаточный этап адаптации базовой/instruct-модели под задачу
или стиль. Эта заметка — прикладной пайплайн: что именно входит в loss, почему
нельзя ошибиться с chat-шаблоном, и как делать SFT экономично через LoRA/QLoRA.
Механика самого LoRA (низкоранговая декомпозиция $\Delta W = BA$) разобрана в DSWoK
§2.7 — здесь **только дельта для практики**: какие модули таргетить, какие
rank/alpha брать, как QLoRA меняет бюджет памяти (`2.5-gpu-minimum`) и как сливать
адаптеры. Преференс-оптимизация (DPO/ORPO/KTO) — следующий этап, см.
`2.2b-preference-optimization`; выбор библиотек — `2.2c-libraries`.

## Суть

SFT — это обычное обучение next-token prediction (как претрейн), но на размеченных
диалогах и с одним критичным отличием: **в loss входят только токены ответа
ассистента**, а токены промпта/системы маскируются. Модель учится *генерировать*
ответы, а не *предсказывать собственный вход*. Два главных источника провала SFT —
не гиперпараметры, а (1) неверный chat-шаблон (чужие спецтокены) и (2) неверное
маскирование loss. Поверх — выбор между full-FT (дорого, см. 16 байт/параметр в
`2.5-gpu-minimum`) и LoRA/QLoRA (дёшево, массовый дефолт).

## Механика

### Что входит в loss: completion-only masking

Обучающий пример — диалог: системный промпт, реплика пользователя, ответ
ассистента. Если считать loss по **всем** токенам, модель тратит ёмкость на
заучивание формулировок пользователя — это и бесполезно, и вредит. Правильно —
**loss masking**: токены промпта помечаются меткой `-100` (PyTorch
`CrossEntropyLoss(ignore_index=-100)` их игнорирует), в loss идут только токены
ответа.

$$
\mathcal{L}_{\text{SFT}} = -\sum_{t \in \text{ответ}} \log p_\theta(y_t \mid y_{<t}, \text{промпт})
$$

- сумма только по позициям `t`, принадлежащим ответу ассистента;
- `y_{<t}` — все предыдущие токены (включая промпт — он в контексте, но не в loss).

На многоходовых диалогах в loss идут токены **всех** ответов ассистента, реплики
пользователя — маскируются. В TRL это `DataCollatorForCompletionOnlyLM` /
`completion_only_loss`.

### Chat-шаблон: почему чужие спецтокены ломают модель

Instruct-модели обучены на конкретной разметке ролей спецтокенами (ChatML
`<|im_start|>`/`<|im_end|>`, Llama-3 `<|start_header_id|>`, Mistral `[INST]` и т.д.).
Эти токены — часть «языка» модели. Если при SFT собрать промпт вручную с **чужими**
или произвольными разделителями, модель учится на распределении, которого не видела
на инференсе → деградация, иногда резкая. Правило: **всегда применять родной
chat-шаблон целевой модели** через `tokenizer.apply_chat_template(...)`, не
конструировать строку руками. Формат данных-де-факто (2026) — JSONL с массивом
`messages` (роли `system`/`user`/`assistant`), шаблон применяет токенизатор.

### Packing: упаковка коротких примеров

Короткие примеры тратят впустую дополненный (padding) хвост seq_len. **Packing**
склеивает несколько примеров в одну последовательность длины `max_len`, разделяя
их EOS, чтобы не простаивали токены. Ускоряет обучение в разы на коротких данных.
Тонкость: нужно следить, чтобы attention не «протекал» между упакованными
примерами (cross-contamination) — современные коллаторы используют block-diagonal
attention mask / `position_ids` сброс.

### LoRA на практике: какие модули таргетить (дельта к DSWoK §2.7)

LoRA замораживает базовые веса и обучает низкоранговую добавку $\Delta W = BA$
(DSWoK §2.7). Прикладные решения, которых нет в теории:

- **target_modules.** Ранняя рекомендация (из статьи LoRA) — только `q_proj`,
  `v_proj`. Современная практика по бенчмаркам: **таргетить все линейные слои** —
  attention (`q,k,v,o_proj`) и MLP (`gate,up,down_proj`); в PEFT это
  `target_modules="all-linear"`. Больше модулей → выше качество, ценой числа
  обучаемых параметров. MLP-слои часто важнее для усвоения знаний, чем только
  attention.
- **rank `r` и `alpha`.** `r` — ранг добавки (ёмкость адаптера): типично 8/16/32/64.
  `alpha` масштабирует добавку (эффективный масштаб $\alpha/r$); рабочий дефолт —
  **`alpha = 2r`** (например `r=16, alpha=32`). Больше `r` → больше ёмкость и
  памяти; для стилевой адаптации хватает `r=8–16`, для усвоения новых навыков —
  `r=32–64`.
- **lora_dropout** ~0.05 для регуляризации; **bias** обычно `none`.

### QLoRA: 4-битная база меняет бюджет памяти

QLoRA (Dettmers et al., arXiv:2305.14314) = LoRA поверх базовой модели,
**квантованной в 4 бита (NF4)**. База заморожена в 4 битах (~0.5 байта/параметр),
обучаются только bf16-адаптеры. Три компонента:
- **NF4 (4-bit NormalFloat)** — формат, информационно-оптимальный для нормально
  распределённых весов;
- **double quantization** — квантуются и сами константы квантизации (экономия
  ~0.4 бита/параметр);
- **paged optimizers** — состояния Adam выгружаются в CPU при пиках памяти (гасит
  OOM на скачках длины).

Эффект для памяти разобран в `2.5-gpu-minimum`: 70B-QLoRA влезает на одну 48–80GB
карту, тогда как full-FT 70B требует ~1.1 ТБ. Цена — обучение чуть медленнее
(деквантизация на лету) и небольшая потеря качества против LoRA в bf16; на практике
QLoRA близок к LoRA по итоговому качеству.

### Слияние адаптеров (merging) и его подвох

Для инференса адаптер можно **слить** в базовые веса: $W' = W + \frac{\alpha}{r}BA$
(`merge_and_unload` в PEFT) — тогда нет оверхеда на отдельный проход адаптера и
модель раздаётся как обычная. **Подвох с QLoRA:** база хранится в 4 битах, а
адаптер в bf16 — слияние в 4-битную базу теряет точность. Корректно: догрузить базу
в fp16/bf16, слить адаптер туда, сохранить fp16-модель (или держать адаптер
отдельно и не сливать). Несколько адаптеров можно держать раздельно и переключать
на лету (мультитенант) — это и есть аргумент против поспешного слияния.

### Full-FT vs LoRA/QLoRA: когда что

| Критерий | Full fine-tuning | LoRA / QLoRA |
|---|---|---|
| Память | ~16 Б/параметр (Adam) | базовые веса заморожены; обучаемых <1% |
| Качество | потолок выше | близко к full для большинства задач |
| Несколько задач | копия модели на задачу | один базовый + N адаптеров |
| Риск катастрофического забывания | выше | ниже (база заморожена) |
| Когда брать | глубокая смена поведения/домена, есть железо | дефолт: адаптация стиля/задачи, ограниченная VRAM |

## Практические соображения

- **Дефолтный рецепт SFT-LoRA:** QLoRA (4-bit NF4) + `target_modules="all-linear"`
  + `r=16, alpha=32, dropout=0.05`, LR ~1–2e-4 (для LoRA выше, чем для full-FT
  ~1–2e-5), cosine schedule + warmup, 1–3 эпохи, completion-only loss, родной
  chat-шаблон.
- **Число эпох мало:** 1–3. Больше → переобучение и забывание (`2.1-pytorch-fluency`,
  чтение loss curves).
- **Проверяй пример после токенизации:** распечатай, что реально пошло в модель
  (спецтокены на месте, промпт замаскирован) — половина багов видна глазами.
- **Качество данных > объём.** 1–10k чистых разнообразных примеров часто бьют
  100k шумных (см. `2.3-data-preparation`, идея LIMA).
- **Эвал отдельно от обучения:** loss падает ≠ задача решается — мерить на
  hold-out задаче-специфично (`1.4-evaluation`).

## Режимы отказа

- **Модель «несёт» формат пользователя, повторяет инструкции.** Не замаскирован
  промпт — loss считается по всему. Симптом: генерит куски промпта. Фикс:
  completion-only masking (`-100` на промпт).
- **Резкая деградация после SFT.** Неверный chat-шаблон / чужие спецтокены. Симптом:
  ломается формат ответа, странные токены. Фикс: `apply_chat_template` целевой
  модели, проверить спецтокены.
- **Слил QLoRA-адаптер — качество просело.** Слияние bf16-адаптера в 4-битную базу.
  Фикс: грузить базу в fp16, слить туда, сохранить fp16.
- **Катастрофическое забывание базовых навыков.** Слишком много эпох / высокий LR /
  узкие данные. Фикс: меньше эпох, ниже LR, подмешать общие данные, предпочесть
  LoRA full-FT.
- **OOM на QLoRA, хотя «должно влезать».** Длинный контекст раздувает активации/KV.
  Фикс: gradient checkpointing, короче `max_len`, packing, меньший batch +
  accumulation (`2.1-pytorch-fluency`).
- **LoRA «не учится» (loss плато).** Слишком мал `r`/`alpha`, не те модули (только
  attention), мал LR. Фикс: `all-linear`, `r≥16`, поднять LR до ~2e-4.

## Код

```python
# QLoRA-SFT: 4-бит база + LoRA на все линейные + completion-only loss.
from transformers import AutoModelForCausalLM, AutoTokenizer, BitsAndBytesConfig
from peft import LoraConfig, get_peft_model
from trl import SFTTrainer, SFTConfig
import torch

bnb = BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4",
                         bnb_4bit_compute_dtype=torch.bfloat16,
                         bnb_4bit_use_double_quant=True)   # NF4 + double quant
tok = AutoTokenizer.from_pretrained(MODEL)
model = AutoModelForCausalLM.from_pretrained(MODEL, quantization_config=bnb,
                                             device_map="auto")
lora = LoraConfig(r=16, lora_alpha=32, lora_dropout=0.05, bias="none",
                  target_modules="all-linear",        # не только q,v — все линейные
                  task_type="CAUSAL_LM")
model = get_peft_model(model, lora)
model.print_trainable_parameters()                     # обычно <1% параметров

# данные: [{"messages":[{"role":"user",...},{"role":"assistant",...}]}, ...]
trainer = SFTTrainer(
    model=model, processing_class=tok, train_dataset=ds,
    args=SFTConfig(max_length=2048, packing=True,      # packing коротких примеров
                   completion_only_loss=True,          # маскировать промпт
                   learning_rate=2e-4, num_train_epochs=2,
                   lr_scheduler_type="cosine", warmup_ratio=0.03,
                   bf16=True, gradient_checkpointing=True),
)
trainer.train()
# Шаблон применяется автоматически из tok; вручную строки НЕ собираем.
```

```python
# Слияние адаптера для инференса — корректно для QLoRA (через fp16-базу).
from peft import PeftModel
base = AutoModelForCausalLM.from_pretrained(MODEL, torch_dtype=torch.float16)  # НЕ 4-bit
merged = PeftModel.from_pretrained(base, "adapter_dir").merge_and_unload()
merged.save_pretrained("merged_fp16")   # раздаётся как обычная модель
```

## Вопросы для самопроверки

1. Почему в SFT loss считают только по токенам ответа? Что конкретно ломается, если
   считать по всем токенам?
2. Чем грозит сборка промпта вручную вместо `apply_chat_template`, и почему модель
   к этому так чувствительна?
3. Почему современная практика таргетит все линейные слои, а не только `q_proj/v_proj`?
   Какой ресурс это тратит?
4. Что означает дефолт `alpha = 2r` и как эффективный масштаб $\alpha/r$ влияет на
   силу адаптера?
5. Перечисли три компонента QLoRA и объясни, какой из них даёт основную экономию
   памяти, а какой гасит OOM на пиках.
6. Почему слияние QLoRA-адаптера прямо в 4-битную базу теряет качество и как сделать
   правильно?
7. Когда выберешь full-FT, несмотря на стоимость, а когда LoRA достаточно?
8. Как packing ускоряет обучение и какой баг он может внести, если не сбросить
   attention-маску между примерами?
9. Почему LR для LoRA (~2e-4) на порядок выше, чем для full-FT (~2e-5)?
10. Loss падает, а задача не решается лучше. Что проверишь и почему loss — плохой
    единственный критерий?

## Ссылки

- [P] Hu et al. — LoRA: Low-Rank Adaptation of LLMs (2021), arXiv:2106.09685
- [P] Dettmers et al. — QLoRA (2023), arXiv:2305.14314 (NF4, double quant, paged opt)
- [D] HuggingFace TRL — SFTTrainer (completion-only loss, packing)
  https://huggingface.co/docs/trl/sft_trainer
- [D] HuggingFace PEFT — LoRA (target_modules, merge_and_unload)
  https://huggingface.co/docs/peft/main/en/conceptual_guides/lora
- [G] Raschka — Practical Tips for Finetuning LLMs Using LoRA
  https://raschka.com/blog/2023/llm-finetuning-lora.html
- Предпосылки: DSWoK §2.7 (механика LoRA/QLoRA); `2.1-pytorch-fluency` (mixed
  precision, checkpointing, loss curves); `2.5-gpu-minimum` (бюджет памяти).
- Дальше: `2.2b-preference-optimization` (DPO/ORPO/KTO); `2.2c-libraries`
  (transformers/peft/trl/axolotl/unsloth); `2.3-data-preparation` (данные для SFT);
  `1.4-evaluation` (как мерить результат).
