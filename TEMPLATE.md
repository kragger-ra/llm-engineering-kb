---
id: "2.4-inference-serving"          # уникальный slug, = имя файла без .md
section: "model"                      # product | model | infra
title: "Инференс и сервинг открытых моделей"
tags: ["vllm", "kv-cache", "quantization", "serving", "throughput"]
prereqs: ["dswok:1.1-attention", "dswok:1.1.5-gqa", "2.5-gpu"]  # что знать до
dswok_refs: ["§1.1", "§1.1.4-1.1.5"]  # на что в исходном DSWoK опираемся
sources:                             # 3-8 первоисточников (см. sources.md)
  - "https://arxiv.org/abs/2309.06180"   # vLLM / PagedAttention
  - "..."
difficulty: "advanced"               # intro | intermediate | advanced
last_reviewed: "2026-05-30"          # дата факт-чека (для волатильных тем)
volatile: true                       # true если факты быстро устаревают
---

# {Заголовок темы}

> 1-2 абзаца: что это, зачем нужно, где в стеке стоит. Без воды.
> Если тема пересекается с DSWoK — явная ссылка и фраза «здесь только дельта».

## Суть

Определение и место в общей картине. Что эта тема решает и какую проблему
закрывает. Связь с соседними темами (по `prereqs`).

## Механика

**Самый важный раздел.** Как это устроено внутри, а не «как вызвать библиотеку».
Обязательно где применимо:
- формулы (KaTeX-совместимый markdown), с расшифровкой каждого символа;
- пошаговый разбор алгоритма/пайплайна;
- диаграмма (mermaid) для архитектур и потоков;
- конкретные числа (размеры, пороги, бенчмарки) с указанием источника.

## Практические соображения

Что реально делают инженеры. Выбор инструментов, дефолтные значения
гиперпараметров, рычаги настройки, типичные диапазоны. Таблицы компромиссов.

## Режимы отказа

Как ломается, по каким симптомам диагностировать, чем чинить. Это отличает
справочник от пересказа документации.

## Код

Минимальный рабочий пример, иллюстрирующий механику (не туториал по библиотеке).
Комментарии объясняют, что и почему, а не что делает строка.

## Вопросы для самопроверки

5-10 вопросов уровня собеседования senior-инженера. Не «что такое X», а «почему
X, а не Y», «что сломается если», «как оценить». Это инструмент проверки
«свободного владения».

## Ссылки

Первоисточники (статьи, доки, бенчмарки) с URL. Помечать, какие быстро устаревают.
Перекрёстные ссылки на другие заметки корпуса и на DSWoK §X.Y.
